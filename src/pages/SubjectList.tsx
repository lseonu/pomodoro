import { css } from "@emotion/react";
import React from "react";

export const SubjectList: React.FC = () => {
  return (
    <div
      css={css`
        font-size: 100px;
        font-weight: bold;
      `}
    >
      subject list
    </div>
  );
};
