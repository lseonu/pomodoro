import { css } from "@emotion/react";
import React from "react";

export const Timer: React.FC = () => {
  return (
    <div
      css={css`
        font-size: 100px;
        font-weight: bold;
      `}
    >
      timer page
    </div>
  );
};
